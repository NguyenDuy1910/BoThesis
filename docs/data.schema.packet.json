{
  "schema_version": 4,
  "title": "BoThesis canonical Item and storage ownership",
  "status": "implemented",
  "ownership": {
    "postgresql": "durable business and application state",
    "object_storage": "original binary source truth",
    "docling": "document understanding, normalization, chunking, and provenance",
    "qdrant": "derived contextual retrieval projection",
    "knowledge": "permission filtering, retrieval, reranking, and evidence",
    "agent": "reasoning over grounded evidence"
  },
  "canonical_entities": {
    "items": ["collection", "document"],
    "hierarchy": "parent_item_id",
    "object_metadata": ["storage_key", "mime_type", "size_bytes"],
    "lifecycle": ["pending", "processing", "ready", "failed", "unsupported", "deleted"]
  },
  "integration_entities": {
    "integration_connections": "reusable connector configuration",
    "integration_credentials": "connection-bound authenticated-encryption payloads",
    "ingestion_sources": "independently checkpointed external scopes targeting Collection Items",
    "external_resources": "external identity and version state mapped to canonical Items",
    "incremental_identity": "(ingestion_source_id, external_id) -> item_id",
    "execution_history": "Temporal"
  },
  "upload_entities": {
    "item_uploads": "native upload idempotency and lifecycle without external-resource tracking"
  },
  "chat_entities": {
    "message_items": ["attachment", "reference", "output"]
  },
  "forbidden_persistence": [
    "raw bytes in PostgreSQL",
    "processed chunks in PostgreSQL",
    "presigned URLs",
    "plaintext connector credentials",
    "generation-based index state"
  ],
  "schema_change": "ORM defines the target schema; SQL migrations preserve durable PostgreSQL state"
}
